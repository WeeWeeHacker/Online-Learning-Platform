@model IEnumerable<CacheClass.Models.LearnerDiscussion>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Time)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Learner.LearnerID)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Time)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Learner.LearnerID)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Learner.LearnerID }) |
                @Html.ActionLink("Details", "Details", new { id = item.Learner.LearnerID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Learner.LearnerID })
            </td>
        </tr>
}
    </tbody>
</table>